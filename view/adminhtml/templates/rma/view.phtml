<?php
/**
 * ViraXpress - https://www.viraxpress.com
 *
 * LICENSE AGREEMENT
 *
 * This file is part of the ViraXpress package and is licensed under the ViraXpress license agreement.
 * You can view the full license at:
 * https://www.viraxpress.com/license
 *
 * By utilizing this file, you agree to comply with the terms outlined in the ViraXpress license.
 *
 * DISCLAIMER
 *
 * Modifications to this file are discouraged to ensure seamless upgrades and compatibility with future releases.
 *
 * @category    ViraXpress
 * @package     ViraXpress_Rma
 * @author      ViraXpress
 * @copyright   © 2024 ViraXpress (https://www.viraxpress.com/)
 * @license     https://www.viraxpress.com/license
 */
/** @var \ViraXpress\Rma\Block\Adminhtml\Rma\View $block */
/** @var \Magento\Framework\Stdlib\DateTime\TimezoneInterface $timezone */
// @phpcs:disable Generic.Files.LineLength

$timezone = $block->getTimezone();

$rma = $block->getRmaData();
$rmaItems=$block->getRmaItemData();
$order = $block->getOrder();
$customer = $block->getCustomer();
$orderItems = [];

foreach ($order->getAllItems() as $orderItem) {
    $orderItems[$orderItem->getProductId()] = $orderItem;
}
$rmaStatusOptions = $block->getRmaStatusOptions();
$itemStatusOptions = $block->getItemStatusOptions();
$billingAddress = $order->getBillingAddress();
$shippingAddress = $order->getShippingAddress();
$mediaBaseUrl = $block->getImageBaseUrl();

?>
<style>
    .page-actions._fixed {
        position: sticky;
        top: 0;
        z-index: 1;
        background-color: #f8f8f8;
        padding: 16px 24px;
        border-bottom: 1px solid #d6d6d6;
        display: flex;
        justify-content: flex-end;
        gap: 10px;
        width: 150%;
        margin-left:-400px;

    }
    .custom-rma-wrapper {
        position:relative;
        left:-250px;
        margin-left: 0 !important;
        padding-left: 0 !important;
        padding-right: 20px;
        width:80dvw;
    }

    .custom-rma-row {
        display: flex;
        justify-content: space-between;
        gap: 40px;
        margin-bottom: 20px;
    }

    .custom-rma-column {
        width: 48%;
    }

    .custom-rma-column h2,
    .custom-rma-column h4 {
        margin-bottom: 8px;
        border-bottom: 1px solid #ccc;
        padding-bottom: 5px;
    }

    .custom-rma-column address {
        font-style: normal;
        line-height: 1.5em;
    }
    .AddressContainer{
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 20px;
        margin-top: 10px;
    }

    .BillingAddress, .ShippingAddress {
        padding: 10px;
        border: 1px solid #ccc;
        border-radius: 6px;
        background-color: #f9f9f9;
    }
    /* Order */
    .custom-rma-order-details {
        margin-top: 30px;
        margin-bottom: 20px;
    }

    .custom-rma-products-grid {
        margin-top: 10px;
    }

    .custom-rma-products-grid table {
        width: 100%;
        border-collapse: collapse;
    }


    
    .custom-admin-select {
        display: inline-block;
        width: 100%;
        max-width: 400px;
        padding: 8px 12px;
        font-size: 1.2rem;
        color: #333;
        background-color: #fff;
        border: 1px solid #adadad;
        border-radius: 4px;
        box-shadow: inset 0 1px 2px rgba(0,0,0,0.1);
        transition: border-color 0.3s ease, box-shadow 0.3s ease;
        appearance: none;
        -webkit-appearance: none;
        -moz-appearance: none;
        background-image: url("data:image/svg+xml;charset=US-ASCII,%3Csvg%20width%3D'10'%20height%3D'5'%20xmlns%3D'http%3A//www.w3.org/2000/svg'%3E%3Cpath%20d%3D'M0%200l5%205%205-5z'%20fill%3D'%23666'%20/%3E%3C/svg%3E");
        background-repeat: no-repeat;
        background-position: right 10px center;
        background-size: 10px 5px;
    }

    .custom-admin-select:focus {
        border-color: #006bb4;
        box-shadow: 0 0 0 2px rgba(0, 107, 180, 0.3);
        outline: none;
    }

    .custom-admin-select:hover {
        border-color: #999;
    }
    .pdf-viewer{
        width:50px;
        height:50px; 
        background-color:transparent;
        position:absolute;
        cursor: pointer;
    }
</style>
<div class="page-actions _fixed">
    <div class="page-actions-inner">
        <div class="page-actions-buttons">
            <button type="button" class="action-default scalable back" id="rma-back">
                <span><?= $escaper->escapeHtml(__('Back')) ?></span>
            </button>
        </div>
    </div>
</div>
<br>
<div class="custom-rma-wrapper">
    <div class="custom-rma-row">
        <!-- Customer Info -->
        <div class="custom-rma-column">
            <h2>Customer Details</h2>
            <p><strong>Name:</strong>
            <?= $escaper->escapeHtml($order->getCustomerName())?>
            </p>
            <p><strong>Email:</strong> <?= $escaper->escapeHtml($order->getCustomerEmail()) ?></p>
            <p><strong>Group:</strong> <?= $escaper->escapeHtml($block->getCustomerGroupName($order->getCustomerGroupId())) ?></p>
        </div>

        <!-- Address Info -->
        <div class="custom-rma-column">
            <h2>Address Details</h2>
            <div class="AddressContainer">
                <div class="BillingAddress">
                    <?php if ($billingAddress): ?>
                        <h4>Billing Address</h4>
                        <address>
                            <?= $escaper->escapeHtml($billingAddress->getFirstname())
                            . ' ' . $escaper->escapeHtml($billingAddress->getLastname()) ?><br/>
                            <?= /** noescape */ implode('<br>', $billingAddress->getStreet()) ?><br/>
                            <?= $escaper->escapeHtml($billingAddress->getCity()) ?>, 
                            <?= $escaper->escapeHtml($billingAddress->getRegion()) ?><br/>
                            <?= $escaper->escapeHtml($billingAddress->getPostcode()) ?>, 
                            <?= $escaper->escapeHtml($billingAddress->getCountryId()) ?><br/>
                            T: <?= $escaper->escapeHtml($billingAddress->getTelephone()) ?>
                        </address>
                    <?php endif; ?>
                </div>
                <div class="ShippingAddress">
                    <?php if ($shippingAddress): ?>
                        <h4>Shipping Address</h4>
                        <address>
                            <?= $escaper->escapeHtml($shippingAddress->getFirstname())
                            . ' ' . $escaper->escapeHtml($shippingAddress->getLastname()) ?><br/>
                            <?= /** noescape */ implode('<br>', $billingAddress->getStreet()) ?><br/>
                            <?= $escaper->escapeHtml($shippingAddress->getCity()) ?>,
                            <?= $escaper->escapeHtml($shippingAddress->getRegion()) ?><br/>
                            <?= $escaper->escapeHtml($shippingAddress->getPostcode()) ?>,
                            <?= $escaper->escapeHtml($shippingAddress->getCountryId()) ?><br/>
                            T: <?= $escaper->escapeHtml($shippingAddress->getTelephone()) ?>
                        </address>
                    <?php endif; ?>
                </div>
            </div>
        </div>
    </div>
    <hr>
    <div class="custom-rma-order-details">
        <?php
        $utcOrderCreatedDate = $order->getCreatedAt();
        $DateOrderCreatedDate = $timezone->date(new \DateTime($utcOrderCreatedDate));
        $OrderCreatedDate = $DateOrderCreatedDate   ->format('Y-m-d H:i:s');
        ?>
        <h2>Order Details</h2>
        <p><strong>Order ID:</strong>
        <a href="<?= $escaper->escapeUrl($block
            ->getUrl('sales/order/view', ['order_id' => $order->getId()])) ?>">
            <?= $escaper->escapeHtml('#'.$order->getIncrementId()) ?>
        </a>
        &nbsp;&nbsp;&nbsp;&nbsp;| &nbsp;&nbsp;&nbsp;&nbsp;
        <strong>Order Date:</strong> <?= $escaper->escapeHtml($OrderCreatedDate) ?>
        &nbsp;&nbsp;&nbsp;&nbsp;|&nbsp;&nbsp;&nbsp;&nbsp;
        <strong>Status:</strong> <?= $escaper->escapeHtml($order->getStatusLabel()) ?>
        &nbsp;&nbsp;&nbsp;&nbsp;|&nbsp;&nbsp;&nbsp;&nbsp;
        <strong>Order Total:</strong> <?= $escaper->escapeHtml($block->formatPrice($order->getGrandTotal())) ?>
        </p>
    </div>
    <div class="custom-rma-products-grid">
        <h2>Ordered Products</h2>
        <table class="data-grid admin__control-table">
            <thead>
                <tr class="data-grid-tr">
                    <th class="data-grid-th"><span><?= $escaper->escapeHtml(__('SKU')) ?></span></th>
                    <th class="data-grid-th"><span><?= $escaper->escapeHtml(__('Product Name')) ?></span></th>
                    <th class="data-grid-th"><span><?= $escaper->escapeHtml(__('Qty Ordered')) ?></span></th>
                    <th class="data-grid-th"><span><?= $escaper->escapeHtml(__('Price')) ?></span></th>
                    <th class="data-grid-th"><span><?= $escaper->escapeHtml(__('Row Total')) ?></span></th>
                </tr>
            </thead>
            <tbody>
                <?php foreach ($order->getAllVisibleItems() as $item): ?>
                    <tr class="data-grid-tr">
                        <td class="data-grid-td"><?= $escaper->escapeHtml($item->getSku()) ?></td>
                        <td class="data-grid-td"><?= $escaper->escapeHtml($item->getName()) ?></td>
                        <td class="data-grid-td"><?= /** noescape */ (int) $item->getQtyOrdered() ?></td>
                        <td class="data-grid-td"><?= /** noescape */ $block->formatPrice($item->getPrice()) ?></td>
                        <td class="data-grid-td"><?= /** noescape */ $block->formatPrice($item->getRowTotal()) ?></td>
                    </tr>
                <?php endforeach; ?>
            </tbody>
        </table>
    </div>
        <br>
        <hr>
        <br>
    <div class="custom-rma-return-details">
        <?php
            $utcRmaCreatedDate = $rma->getCreatedAt();
            $DateRmaCreatedDate = $timezone->date(new \DateTime($utcRmaCreatedDate));
            $RmaCreatedDate = $DateRmaCreatedDate ->format('Y-m-d H:i:s');
        ?>
        <h2>Return Request Details</h2>
        <p style="width:100%;"><strong>RMA ID:</strong> <?= $escaper->escapeHtml($rma->getRmaId()) ?>
        &nbsp;&nbsp;&nbsp;&nbsp;| &nbsp;&nbsp;&nbsp;&nbsp;
        <strong>Date Requested:</strong> <?= $escaper->escapeHtml($RmaCreatedDate) ?>
        &nbsp;&nbsp;&nbsp;&nbsp;| &nbsp;&nbsp;&nbsp;&nbsp;
        <strong>Status:</strong> 
                <select style="width:250px !important;"  name="rma_status" 
                    class="custom-admin-select rma_status"  
                    data-rma-id="<?= $escaper->escapeHtml($rma->getRmaId()) ?>" >
                    <?php foreach ($rmaStatusOptions as $status): ?>
                        <option value="<?= $escaper->escapeHtml($status) ?>" 
                        <?= $escaper->escapeHtml($rma->getStatus()) ===
                            $escaper->escapeHtml($status) ? 'selected' : '' ?>>
                            <?= $escaper->escapeHtml($status) ?>
                        </option>
                    <?php endforeach; ?>
                </select>
    </div>
    <div class="custom-rma-products-grid">
        <h2>Returned Items</h2>
            <table  class="data-grid admin__control-table">
                <thead>
                    <tr class="data-grid-tr">
                        <th class="data-grid-th">Product Name</th>
                        <th class="data-grid-th">SKU</th>
                        <th class="data-grid-th">Price</th>
                        <th class="data-grid-th">Qty Requested</th>
                        <th class="data-grid-th">Total</th>
                        <th class="data-grid-th">Reason</th>
                        <th class="data-grid-th">Condition</th>
                        <th class="data-grid-th">Resolution</th>
                        <th class="data-grid-th">Attachments</th>
                        <th class="data-grid-th">Status</th>
                        <th class="data-grid-th">Action</th>
                    </tr>
                </thead>
                <tbody>
                    <?php foreach ($rmaItems as $item): ?>
                        <?php
                        $productId = $item->getProductId();
                        $orderItem = $orderItems[$productId] ?? null;
                        $name = $orderItem ? $orderItem->getName() : 'N/A';
                        $sku = $orderItem ? $orderItem->getSku() : 'N/A';
                        $price = $orderItem ? (float) $orderItem->getPrice() : 0;
                        $qty = (int) $item->getQtyRequested();
                        $total = $price * $qty;
                        $imageUrls = [];
                        $images = $block->getItemImages($item->getId());
                        foreach ($images as $image) {
                            $imageName = $image->getImageName();
                            $imageUrls[] = $mediaBaseUrl . 'rma/uploads/' . $imageName;
                        }
                        $replacedOrder=$block->getReplacedOrder($item->getReplacementOrderId());
                        ?>
                        <tr class="data-grid-tr">
                            <td class="data-grid-td"><?= $escaper->escapeHtml($name) ?></td>
                            <td class="data-grid-td"><?= $escaper->escapeHtml($sku) ?></td>
                            <td class="data-grid-td"><?= $escaper->escapeHtml($block->formatPrice($price)) ?></td>
                            <td class="data-grid-td"><?= $escaper->escapeHtml($qty) ?></td>
                            <td class="data-grid-td"><?= $escaper->escapeHtml($block->formatPrice($total)) ?></td>
                            <td class="data-grid-td"><?= $escaper->escapeHtml($item->getReason()) ?></td>
                            <td class="data-grid-td"><?= $escaper->escapeHtml($item->getCondition()) ?></td>
                            <td class="data-grid-td">
                                <?= $escaper->escapeHtml($item->getResolution()) ?><br>
                                <?php if ($replacedOrder && $replacedOrder->getId()): ?>
                                    <a href="<?= $escaper->escapeUrl($block
                                    ->getUrl('sales/order/view') . '?order_id=' . $replacedOrder->getId()) ?>">
                                        <?= $escaper->escapeHtml('#' . $replacedOrder->getIncrementId()) ?>
                                    </a>
                                <?php else: ?>
                                    <span>-</span>
                                <?php endif; ?>
                            </td>
                            <td class="data-grid-td">
                                <?php foreach ($imageUrls ?? [] as $fileUrl):
                                    $fileInfo = $block->getPathInfo($fileUrl);
                                    $fileName  = $fileInfo['basename'];
                                    $extension = strtolower($fileInfo['extension']);
                                    $isImage   = in_array($extension, ['jpg', 'jpeg', 'png', 'gif', 'bmp', 'webp']);
                                    $isPdf     = $extension === 'pdf';
                                    if ($isImage):
                                        ?>
                                    <img src="<?= $escaper->escapeUrl($fileUrl) ?>"
                                        data-full="<?= $escaper->escapeUrl($fileUrl) ?>"
                                        class="rma-image-thumb"
                                        alt="RMA Image"
                                        style="width: 50px;height: auto;margin: 2px;cursor: pointer;" />
                                <?php elseif ($isPdf): ?>
                                    <div style="" class="pdf-viewer rma-image-thumb" 
                                    data-full="<?= $escaper->escapeUrl($fileUrl) ?>">
                                    </div>
                                    <embed src="<?= $escaper->escapeUrl($fileUrl) ?>"
                                        type="application/pdf"
                                        width="50"
                                        height="50"
                                        style="margin: 2px;
                                        cursor: pointer;" />
                                <?php else: ?>
                                    <div class="rma-file-box"
                                        style="width: 50px; height: 50px; background: #eee; border: 1px solid #ccc; display: flex; flex-direction: column; align-items: center; justify-content: center; font-size: 10px; padding: 2px; margin: 2px; text-align: center;">
                                        <span style="font-size: 14px;">📎</span>
                                        <a href="<?= $escaper->escapeUrl($fileUrl) ?>" target="_blank"
                                        title="Download or View <?= $escaper->escapeHtml($fileName) ?>"
                                        style="color: #333; text-decoration: none;">
                                            <?= $escaper->escapeHtml(mb_strimwidth($fileName, 0, 10, '...')) ?>
                                        </a>
                                    </div>
                                <?php endif; ?>
                            <?php endforeach; ?>
                            </td>
                            <td class="data-grid-td" style="width:150px;">
                            <?php if ($rma->getStatus() != 'Approved' && $rma->getStatus() != 'Pending'
                                    && $rma->getStatus() != 'Cancelled'  && $item->getStatus() != 'Cancelled'): ?>
                                <select name="item_status" class="custom-admin-select item_status"
                                    data-item-id="<?= $escaper->escapeHtmlAttr($item->getId()) ?>"
                                    data-rma-id="<?= $escaper->escapeHtmlAttr($rma->getId()) ?>">
                                    <?php foreach ($itemStatusOptions as $status): ?>
                                        <option value="<?= $escaper->escapeHtmlAttr($status) ?>"
                                            <?= $item->getStatus() === $status ? 'selected' : '' ?>>
                                            <?= $escaper->escapeHtml($status) ?>
                                        </option>
                                    <?php endforeach; ?>
                                </select>
                            <?php else: ?>
                                <select class="custom-admin-select item_status" disabled>
                                    <option selected>
                                        <?= $escaper->escapeHtml($item->getStatus()) ?>
                                    </option>
                                </select>
                            <?php endif; ?>
                            </td> 
                            <td class="data-grid-td" style="width:150px;height:150px;display:grid;gap:10px;">
                                <?php
                                    $isItemStatusValid = $item->getStatus() === 'Inspection In Progress';
                                    $isRmaStatusValid = $rma->getStatus() === 'Received';
                                    $isMoved=$item->getIsMovedToInspection()==0;
                                    $canMoveToInspection = $isItemStatusValid && $isRmaStatusValid && $isMoved;
                                ?>

                                <?php if ($canMoveToInspection): ?>
                                    <!-- Real Actionable Button -->
                                    <button class="inspection-action-button"
                                        data-item-id="<?= $escaper->escapeHtmlAttr($item->getId()) ?>"
                                        data-product-id="<?= $escaper->escapeHtmlAttr($item->getProductId()) ?>"
                                        data-sku="<?= $escaper->escapeHtmlAttr($sku) ?>"
                                        data-qty="<?= $escaper->escapeHtmlAttr($item->getQtyRequested()) ?>"
                                        data-rma-id="<?= $escaper->escapeHtmlAttr($rma->getId()) ?>"
                                        data-condition="<?= $escaper->escapeHtmlAttr($item->getCondition()) ?>">
                                        Move to Inspection
                                    </button>
                                <?php else: ?>
                                    <button class="inspection-button" disabled>
                                        Move to Inspection
                                    </button>
                                <?php endif; ?>

                            <?php
                                $isRmaReceived = $rma->getStatus() === 'Received';
                                $isStatusAccepted = $item->getStatus() === 'Return Accepted';
                            ?>

                                <?php if ($item->getResolution() === 'Refund'): ?>
                                    <?php $canRefund = $isRmaReceived
                                                       && $isStatusAccepted
                                                       && $item->getIsReturned() === '0'; ?>

                                    <?php if ($canRefund): ?>
                                        <button class="refund-action-button"
                                            data-item-id="<?= $escaper->escapeHtmlAttr($item->getItemId()) ?>"
                                            data-order-id="<?= $escaper->escapeHtmlAttr($order->getId()) ?>"
                                            data-product-id="<?= $escaper->escapeHtmlAttr($item->getProductId()) ?>"
                                            data-qty="<?= $escaper->escapeHtmlAttr($item->getQtyRequested()) ?>"
                                            data-rma-id="<?= $escaper->escapeHtmlAttr($rma->getId()) ?>">
                                            Refund
                                        </button>
                                    <?php else: ?>
                                        <button class="refund-action-button" disabled>Refund</button>
                                    <?php endif; ?>
                                <?php endif; ?>


                                <?php if ($item->getResolution() === 'Replacement'): ?>
                                    <?php $canReplace = $isRmaReceived
                                                        && $isStatusAccepted
                                                        && $item->getIsReplaced() === '0'; ?>

                                    <?php if ($canReplace): ?>
                                        <button class="replacement-action-button"
                                            data-item-id="<?= $escaper->escapeHtmlAttr($item->getId()) ?>"
                                            data-order-id="<?= $escaper->escapeHtmlAttr($order->getId()) ?>"
                                            data-product-id="<?= $escaper->escapeHtmlAttr($item->getProductId()) ?>"
                                            data-qty="<?= $escaper->escapeHtmlAttr($item->getQtyRequested()) ?>"
                                            data-rma-id="<?= $escaper->escapeHtmlAttr($rma->getId()) ?>">
                                            Replacement
                                        </button>
                                    <?php else: ?>
                                        <button class="replacement-button" disabled>Replacement</button>
                                    <?php endif; ?>
                                <?php endif; ?>
                            </td>
                        </tr>
                    <?php endforeach; ?>
                </tbody>
            </table>
    </div>
</div>
<div id="rma-image-modal" style="display:none;">
    <div id="rma-image-modal-content" style="text-align: center;"></div>
</div>

<script type="text/x-magento-init">
{
    "*": {
        "ViraXpress_Rma/js/rma-view": {
            "backUrl": "<?= $escaper->escapeUrl($block->getUrl('vx_rma/rma/index')) ?>",
            "updateStatusUrl": "<?= $escaper->escapeUrl($block->getUrl('vx_rma/rma/updatestatus')) ?>",
            "updateItemStatusUrl": "<?= $escaper->escapeUrl($block->getUrl('vx_rma/rma/updateitemstatus')) ?>",
            "refundUrl": "<?= $escaper->escapeUrl($block->getUrl('vx_rma/rma/refund')) ?>",
            "replacementUrl": "<?= $escaper->escapeUrl($block->getUrl('vx_rma/rma/replacement')) ?>",
            "inspectionUrl": "<?= $escaper->escapeUrl($block->getUrl('vx_rma/rma/movetoinspection')) ?>",
            "customerEmail": "<?= $escaper->escapeJs($customer->getEmail()); ?>",
            "customerName": "<?= $escaper->escapeJs($customer->getFirstname() . ' ' . $customer->getLastname()); ?>"
        }
    }
}
</script>
